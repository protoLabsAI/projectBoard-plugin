The blocked sweep no longer logs "operator notified" at WARNING on every pass for a card the
inbox already holds. It said so every five minutes even though the alert was deduped to one
inbox item; a deduped pass now logs at DEBUG.
