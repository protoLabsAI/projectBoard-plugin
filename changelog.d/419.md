Remove the "stale db_path override" advisory. It fired on every multi-project board and told
operators to delete a `db_path` line that was not in their config: the check tested `"db_path"
in cfg`, but this plugin's own manifest declares `db_path: ""`, so the key is always present.
The pre-D3 migration risk it was meant to cover is already reported precisely by the
`db_legacy` advisory, which names actual repos still carrying a `.beads/` workspace.
