# The board and Configure views are assembled at import time from source files
# under view/. They are NOT .py files, so an sdist would drop them without this
# include — keep both sandboxed pages renderable from an installed plugin.
recursive-include view *.html *.css *.js
